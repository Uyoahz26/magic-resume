/**
 * i18n 兼容层 - navigation helpers (stub)
 *
 * 原 next-intl 的 Link / useRouter / usePathname 包装暂时不复用,
 * 直接 re-export 项目已有的 navigation helpers。
 */

export { usePathname, useRouter, Link } from "@/lib/navigation";
